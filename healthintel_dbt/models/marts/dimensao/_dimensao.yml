version: 2

models:
  - name: dim_operadora_atual
    description: Dimensao atual da operadora usada pela API e fatos.
    columns:
      - name: operadora_id
        data_tests:
          - not_null
          - unique
      - name: registro_ans
        data_tests:
          - not_null
          - unique
  - name: dim_competencia
    description: Calendario de competencias disponiveis nas fontes mensais.
    columns:
      - name: competencia
        data_tests:
          - not_null
          - unique
  - name: dim_localidade
    description: Dimensao geografica com municipios IBGE, regiao e populacao estimada.
    columns:
      - name: cd_municipio
        data_tests:
          - not_null
          - unique
      - name: pop_estimada_ibge
        data_tests:
          - not_null
