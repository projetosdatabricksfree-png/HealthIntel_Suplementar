# Em desenvolvimento, deixe vazio para usar o proxy do Vite para http://localhost:8080.
# Em produção, use a URL pública da API ou sirva frontend e API no mesmo domínio/reverse proxy.
VITE_API_BASE_URL=
